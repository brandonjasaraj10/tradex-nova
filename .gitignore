# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env

# Supabase CLI local cache (project link info, generated per machine)
supabase/.temp

# Vite crash-recovery config snapshots
vite.config.ts.timestamp-*.mjs
.tools/

# Claude Code local config - both files hold absolute paths tied to a
# single machine/session (launch.json points at a run-dev.sh inside that
# session's temp scratchpad, which is why the dev server broke once the
# session changed). Nothing here is portable to another checkout.
.claude/
